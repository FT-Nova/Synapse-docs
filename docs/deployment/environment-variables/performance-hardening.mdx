---
title: Performance + Hardening Variables
---

# Performance + Hardening Variables

### SPRING_JPA_PROPERTIES_HIBERNATE_JDBC_BATCH_SIZE

**Description**: JDBC batch size for inserts

**Type**: Integer

**Default**: `20`

**Example**:
```bash
SPRING_JPA_PROPERTIES_HIBERNATE_JDBC_BATCH_SIZE=50
```

---

### SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE

**Description**: Maximum database connection pool size

**Type**: Integer

**Default**: `20`

**Example**:
```bash
SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE=50
```

**Recommendation**:
- Small deployment: 10-20
- Medium deployment: 20-50
- Large deployment: 50-100

---

### SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE

**Description**: Minimum idle connections in pool

**Type**: Integer

**Default**: `5`

**Example**:
```bash
SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE=10
```

---

### SPRING_DATA_REDIS_LETTUCE_POOL_MAX_ACTIVE

**Description**: Maximum active Redis connections in the Lettuce pool

**Type**: Integer

**Default**: `32`

**Example**:
```bash
SPRING_DATA_REDIS_LETTUCE_POOL_MAX_ACTIVE=32
```

---

### SPRING_DATA_REDIS_LETTUCE_POOL_MAX_IDLE

**Description**: Maximum idle Redis connections

**Type**: Integer

**Default**: `16`

**Example**:
```bash
SPRING_DATA_REDIS_LETTUCE_POOL_MAX_IDLE=16
```

---

### SPRING_DATA_REDIS_LETTUCE_POOL_MIN_IDLE

**Description**: Minimum idle Redis connections

**Type**: Integer

**Default**: `4`

**Example**:
```bash
SPRING_DATA_REDIS_LETTUCE_POOL_MIN_IDLE=4
```

---

### SYNAPSE_HTTP_POOL_MAX_THREADS

**Description**: Max worker threads for pooled outbound HTTP client used by model providers

**Type**: Integer

**Default**: `64`

**Example**:
```bash
SYNAPSE_HTTP_POOL_MAX_THREADS=64
```

---

### SYNAPSE_HTTP_CONNECT_TIMEOUT_SECONDS

**Description**: Connection timeout (seconds) for pooled outbound provider HTTP calls

**Type**: Integer

**Default**: `10`

**Example**:
```bash
SYNAPSE_HTTP_CONNECT_TIMEOUT_SECONDS=10
```

---

### SYNAPSE_MEMORY_AUTO_PROMOTE_REUSE_THRESHOLD

**Description**: Number of reads before `SHORT_TERM` memory auto-promotes to `KNOWLEDGE`

**Type**: Integer

**Default**: `5`

**Example**:
```bash
SYNAPSE_MEMORY_AUTO_PROMOTE_REUSE_THRESHOLD=5
```

---

### SYNAPSE_MEMORY_KNOWLEDGE_COMPACTION_DAYS

**Description**: Age threshold (days) used by monthly knowledge-to-archive compaction

**Type**: Integer

**Default**: `30`

**Example**:
```bash
SYNAPSE_MEMORY_KNOWLEDGE_COMPACTION_DAYS=30
```

---

### SYNAPSE_MEMORY_MONTHLY_KNOWLEDGE_CRON

**Description**: Cron expression for monthly knowledge compaction job

**Type**: Cron expression (Spring format)

**Default**: `0 0 3 1 * *`

**Example**:
```bash
SYNAPSE_MEMORY_MONTHLY_KNOWLEDGE_CRON="0 0 3 1 * *"
```

---

### SYNAPSE_MEMORY_BIMONTHLY_ARCHIVE_CRON

**Description**: Cron expression for bi-monthly archive cleanup/compaction scan

**Type**: Cron expression (Spring format)

**Default**: `0 0 3 1 */2 *`

**Example**:
```bash
SYNAPSE_MEMORY_BIMONTHLY_ARCHIVE_CRON="0 0 3 1 */2 *"
```

---

### SYNAPSE_TOOLS_ENABLED

**Description**: Enable native Java tool framework endpoints and runtime execution

**Type**: Boolean

**Default**: `true`

**Example**:
```bash
SYNAPSE_TOOLS_ENABLED=true
```

---

### SYNAPSE_TOOLS_EXECUTION_TIMEOUT_MS

**Description**: Max tool execution time before timeout error

**Type**: Integer (milliseconds)

**Default**: `10000`

**Example**:
```bash
SYNAPSE_TOOLS_EXECUTION_TIMEOUT_MS=10000
```

---

### SYNAPSE_TOOLS_CACHE_ENABLED

**Description**: Enable deterministic native tool result caching

**Type**: Boolean

**Default**: `true`

**Example**:
```bash
SYNAPSE_TOOLS_CACHE_ENABLED=true
```

---

### SYNAPSE_TOOLS_CACHE_DEFAULT_TTL_SECONDS

**Description**: Default cache TTL (seconds) for cacheable native tools without explicit tool TTL override

**Type**: Integer

**Default**: `300`

**Example**:
```bash
SYNAPSE_TOOLS_CACHE_DEFAULT_TTL_SECONDS=300
```

---

### SYNAPSE_HARDENING_ENABLED

**Description**: Enable central hardening policy evaluation across collaboration, planning, and tool execution

**Type**: Boolean

**Default**: `true`

**Example**:
```bash
SYNAPSE_HARDENING_ENABLED=true
```

---

### SYNAPSE_HARDENING_DELEGATION_MAX_HOPS

**Description**: Maximum allowed delegation chain depth before delegation is blocked

**Type**: Integer

**Default**: `6`

**Example**:
```bash
SYNAPSE_HARDENING_DELEGATION_MAX_HOPS=6
```

---

### SYNAPSE_HARDENING_PLANNING_MAX_STEPS

**Description**: Maximum number of steps allowed in a single planning artifact

**Type**: Integer

**Default**: `20`

**Example**:
```bash
SYNAPSE_HARDENING_PLANNING_MAX_STEPS=20
```

---

### SYNAPSE_HARDENING_PLANNING_MAX_REFINEMENTS

**Description**: Maximum plan refinement versions allowed per planning goal

**Type**: Integer

**Default**: `5`

**Example**:
```bash
SYNAPSE_HARDENING_PLANNING_MAX_REFINEMENTS=5
```

---

### SYNAPSE_HARDENING_TOKEN_DEFAULT_BUDGET

**Description**: Default token budget per policy scope window when no phase-specific budget applies

**Type**: Integer

**Default**: `12000`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_DEFAULT_BUDGET=12000
```

---

### SYNAPSE_HARDENING_TOKEN_SCOPE_WINDOW_MINUTES

**Description**: Sliding window length (minutes) for token-budget accounting

**Type**: Integer

**Default**: `60`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_SCOPE_WINDOW_MINUTES=60
```

---

### SYNAPSE_HARDENING_TOKEN_CONCISE_THRESHOLD_RATIO

**Description**: Budget ratio where responses switch to concise-mode warnings before hard block

**Type**: Decimal

**Default**: `0.75`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_CONCISE_THRESHOLD_RATIO=0.75
```

---

### SYNAPSE_HARDENING_TOKEN_PLANNING_BUDGET

**Description**: Token budget for planning-phase operations per scope window

**Type**: Integer

**Default**: `4000`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_PLANNING_BUDGET=4000
```

---

### SYNAPSE_HARDENING_TOKEN_COLLABORATION_BUDGET

**Description**: Token budget for collaboration-phase operations per scope window

**Type**: Integer

**Default**: `4000`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_COLLABORATION_BUDGET=4000
```

---

### SYNAPSE_HARDENING_TOKEN_TOOLING_BUDGET

**Description**: Token budget for tooling-phase operations per scope window

**Type**: Integer

**Default**: `4000`

**Example**:
```bash
SYNAPSE_HARDENING_TOKEN_TOOLING_BUDGET=4000
```

